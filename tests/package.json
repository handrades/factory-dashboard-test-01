{
  "name": "@factory-dashboard/tests",
  "version": "1.0.0",
  "description": "Integration and performance tests for the factory dashboard system",
  "scripts": {
    "test:integration": "jest --config=jest.integration.config.js",
    "test:performance": "jest --config=jest.performance.config.js",
    "test:all": "npm run test:integration && npm run test:performance",
    "test:watch": "jest --config=jest.integration.config.js --watch",
    "test:coverage": "jest --config=jest.integration.config.js --coverage"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/jest": "^29.5.0",
    "typescript": "~5.8.3",
    "ts-node": "^10.9.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0"
  },
  "dependencies": {
    "@factory-dashboard/shared-types": "file:../services/shared-types"
  },
  "license": "MIT"
}